name: CI

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: |
          cd my-agenda-app
          npm install

      - name: Run Lint
        run: |
          cd my-agenda-app
          npm run lint

      - name: Run Type Check
        run: |
          cd my-agenda-app
          npm run check

      - name: Build Project
        run: |
          cd my-agenda-app
          npm run build

      - name: Run Playwright Tests
        run: |
          cd my-agenda-app
          npm run test:e2e
        env:
          CI: true

      - name: Upload Playwright Test Artifacts (if failed)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-results
          path: my-agenda-app/test-results/
